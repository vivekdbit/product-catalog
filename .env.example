# Product Catalog - Environment Configuration
# Copy this file to .env and update the values

# ===========================================
# Application Configuration
# ===========================================
NODE_ENV=development
API_VERSION=v1

# ===========================================
# Port Configuration
# ===========================================
FRONTEND_PORT=3000
BACKEND_PORT=3001
DB_PORT=5432
REDIS_PORT=6379

# ===========================================
# Database Configuration
# ===========================================
# Note: Use service names (postgres, redis) for Docker internal communication
# Use localhost for external connections from host machine
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your_secure_password_here 
DB_NAME=product_catalog

# SSL Configuration for Database
# Set to "true" only for production with SSL-enabled database
# For local Docker development, keep this as "false" or omit it
DB_SSL=false

# ===========================================
# Redis Configuration
# ===========================================
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password_here

# ===========================================
# Frontend Configuration
# ===========================================
FRONTEND_URL=http://localhost:3000
VITE_API_URL=http://localhost:3001

# ===========================================
# Logging Configuration
# ===========================================
# Available levels: ERROR, WARN, INFO, DEBUG
LOG_LEVEL=INFO

# ===========================================
# Security Configuration
# ===========================================
# IMPORTANT: Generate secure passwords before deploying!
# 
# Generate secure passwords with:
#   openssl rand -base64 32
#
# For production, consider:
#   - Using Docker secrets
#   - Using environment-specific .env files
#   - Storing sensitive data in a secure vault
#
# Replace 'your_secure_password_here' and 'your_redis_password_here'
# with actual secure passwords generated using the command above.

# ===========================================
# Development Overrides
# ===========================================
# For local development without Docker, use:
# DB_HOST=localhost
# REDIS_HOST=localhost
# NODE_ENV=development
# LOG_LEVEL=DEBUG